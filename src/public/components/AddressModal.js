import React from 'react';

const AddressModal = ({ hideAddressModal }) => (
  <div className="addressModal-container">
    AddressModal
    <button onClick={() => hideAddressModal()}>x</button>
  </div>
);

export default AddressModal;
